{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compose up (debug)",
            "type": "shell",
            "command": "docker-compose -f ${cwd}/src/docker-compose.debug.yml up --detach --build",
            "group": "build",
            "problemMatcher": []
        },
        {
            "label": "compose down (debug)",
            "type": "shell",
            "command": "docker-compose -f ${cwd}/src/docker-compose.debug.yml down",
            ,
            "group": "build",
            "problemMatcher": []
        }
    ]
}